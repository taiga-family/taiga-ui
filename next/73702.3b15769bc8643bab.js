(self.webpackChunkdemo=self.webpackChunkdemo||[]).push([[73702],{73702:e=>{e.exports="import {AsyncPipe, DecimalPipe} from '@angular/common';\nimport {Component} from '@angular/core';\nimport {changeDetection} from '@demo/emulate/change-detection';\nimport {encapsulation} from '@demo/emulate/encapsulation';\nimport type {TuiZoomEvent} from '@taiga-ui/cdk';\nimport {tuiClamp, TuiZoom} from '@taiga-ui/cdk';\nimport {map, scan, startWith, Subject} from 'rxjs';\n\n@Component({\n    standalone: true,\n    exportAs: \"Example1\",\n    imports: [TuiZoom, AsyncPipe, DecimalPipe],\n    templateUrl: './index.html',\n    styleUrls: ['./index.less'],\n    encapsulation,\n    changeDetection,\n})\nexport default class Example {\n    protected readonly delta$ = new Subject<number>();\n\n    protected readonly scale$ = this.delta$.pipe(\n        scan((scale, next) => tuiClamp(scale + next, 0.5, 3), 1),\n        startWith(1),\n    );\n\n    protected readonly transform$ = this.scale$.pipe(map((scale) => `scale(${scale})`));\n\n    protected onZoom({delta}: TuiZoomEvent): void {\n        this.delta$.next(delta);\n    }\n}\n"}}]);