/**
 * @deprecated use join type {@link TuiStatusT}
 */
export const enum TuiStatus {
    Default = `default`,
    Primary = `primary`,
    Custom = `custom`,
    Success = `success`,
    Error = `error`,
    Warning = `warning`,
}
