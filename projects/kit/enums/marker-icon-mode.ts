/**
 * @deprecated use join type {@link TuiMarkerIconModeT}
 */
export const enum TuiMarkerIconMode {
    Link = `link`,
    Primary = `primary`,
    Warning = `warning`,
    White = `white`,
    Secondary = `secondary`,
    Success = `success`,
    Error = `error`,
    OnDark = `onDark`,
}
