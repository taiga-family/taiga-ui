/**
 * @internal
 */
export const incorrectQuantumMessage = 'Quantum must be positive';
export const incorrectMaxMessage = 'Min value must be less than max value';
export const incorrectLengthMessage = 'Length must be positive and bigger than index';
export const progressIncorrectMaxValueMessage =
    'In separated mode maxValue required to be less than 10';
