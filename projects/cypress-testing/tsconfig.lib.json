{
    "extends": "../../tsconfig.build.json",
    "compilerOptions": {
        "typeRoots": ["node_modules/@types", "src/commands"],
        "types": ["cypress", "node"],
        "strict": false
    }
}
