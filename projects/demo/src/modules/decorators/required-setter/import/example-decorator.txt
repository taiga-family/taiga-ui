@Input()
@tuiRequiredSetter(
    quantity => Number.isInteger(quantity) && quantity >= 10,
    'Should be integer number more than min value',
)
set quantity(quantity: number | undefined) {
    this.items = new Array(quantity ?? 0).fill(Math.floor(Math.random() * Math.floor(100)));
}

items: Array<number>;
