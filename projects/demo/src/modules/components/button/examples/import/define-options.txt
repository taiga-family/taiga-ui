import {TUI_BUTTON_OPTIONS} from '@taiga-ui/core';

...

@NgModule({
    providers: [
        {
            provide: TUI_BUTTON_OPTIONS,
            useValue: {
                icon: 'tuiIconCheckCircleLarge',
                appearance: 'flat',
                itemSize: 'm',
            },
        },
    ]
...
