/**
 * @internal used in calendar, year picker and wrapper
 */
export const enum TuiInteractiveState {
    Disabled = `disabled`,
    Readonly = `readonly`,
    Active = `active`,
    Hover = `hover`,
}
