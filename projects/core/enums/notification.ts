// TODO: change type in v4.0
// eslint-disable-next-line no-restricted-syntax
export enum TuiNotification {
    Info = `info`,
    Warning = `warning`,
    Success = `success`,
    Error = `error`,
}
