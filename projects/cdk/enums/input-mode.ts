/**
 * @deprecated use join type {@link TuiInputModeT}
 */
export const enum TuiInputMode {
    None = `none`,
    Text = `text`,
    Decimal = `decimal`,
    Numeric = `numeric`,
    Tel = `tel`,
    Email = `email`,
    Url = `url`,
    Search = `search`,
}
