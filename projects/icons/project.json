{
    "name": "icons",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "projects/icons",
    "projectType": "library",
    "prefix": "tui",
    "implicitDependencies": ["!testing"],
    "targets": {
        "build": {
            "executor": "@angular-devkit/build-angular:ng-packagr",
            "outputs": ["{workspaceRoot}/dist/icons"],
            "options": {
                "tsConfig": "tsconfig.build.json",
                "project": "{projectRoot}/ng-package.json"
            }
        },
        "preprocess": {
            "executor": "nx:run-commands",
            "options": {
                "command": "ts-node ./{projectRoot}/scripts/prepare-feather-icons.ts && ts-node ./scripts/preprocess-icons.ts"
            }
        },
        "prebuild": {
            "executor": "nx:run-commands",
            "options": {
                "command": "ts-node ./scripts/compile-icons.ts"
            },
            "dependsOn": [
                {
                    "target": "preprocess",
                    "params": "forward"
                }
            ]
        },
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/icons"],
            "options": {
                "jestConfig": "jest.config.ts",
                "collectCoverage": false,
                "testMatch": ["<rootDir>/{projectRoot}/**/*.spec.ts"]
            }
        },
        "publish": {
            "executor": "nx:run-commands",
            "options": {
                "command": "ts-node ./scripts/publish.ts --path ./dist/icons --dry-run {args.dry-run} --customVersion {args.customVersion} --customTag {args.customTag}"
            }
        }
    }
}
