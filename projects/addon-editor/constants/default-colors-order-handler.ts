import {KeyValue} from '@angular/common';

/**
 * @description: preserve original property order
 */
export function tuiDefaultColorsOrderHandler(
    _a: KeyValue<string, string>,
    _b: KeyValue<string, string>,
): number {
    return 0;
}
