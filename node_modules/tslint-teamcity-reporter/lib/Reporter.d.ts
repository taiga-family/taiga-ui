import { AbstractFormatter } from 'tslint/lib/language/formatter/abstractFormatter';
import { RuleFailure, IFormatterMetadata } from 'tslint';
export declare class Formatter extends AbstractFormatter {
    static metadata: IFormatterMetadata;
    format(failures: RuleFailure[], config?: {
        [key: string]: string;
    }): string;
}
