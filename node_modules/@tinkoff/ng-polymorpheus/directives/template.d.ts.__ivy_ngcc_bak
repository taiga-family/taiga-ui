import { ChangeDetectorRef, TemplateRef } from '@angular/core';
/**
 * ng-template wrapper directive also storing {@link ChangeDetectorRef} to properly handle change detection.
 */
export declare class PolymorpheusTemplate<T extends object> {
    readonly template: TemplateRef<T>;
    private readonly changeDetectorRef;
    constructor(template: TemplateRef<T>, changeDetectorRef: ChangeDetectorRef);
    check(): void;
}
