import { FactoryProvider } from '@angular/core';
/** For older version of TS and Angular that do not support all properties from Navigator */
interface NavigatorLike extends Navigator {
    [key: string]: any;
}
export declare function navigatorFactory(userAgent: string | null): NavigatorLike;
export declare const UNIVERSAL_NAVIGATOR: FactoryProvider;
export {};
