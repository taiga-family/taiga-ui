name: 🤖 Screenshot Report
on:
  workflow_run:
    workflows: [⚙️ E2E testing]
    types: [requested, completed]
    branches-ignore:
      - 'v[0-9].x'
      - 'release/**'
  pull_request:
    types: [closed]

jobs:
  awake-screenshot-bot:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: write
      pull-requests: write
    steps:
      - uses: taiga-family/argus@f28f0914e28530403f61aab7080d3de1fd0c9284 # main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
