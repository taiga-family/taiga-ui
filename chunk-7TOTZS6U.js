import"./chunk-HU6DUUP4.js";var n=`.tree._dragged {
    .drop {
        pointer-events: auto;

        &:hover {
            opacity: 1;
        }
    }
}

.wrapper {
    position: relative;
    inline-size: 100%;
}

.content {
    display: flex;
    inline-size: 100%;
    align-items: center;
}

.tiles {
    inline-size: 100%;
    grid-template-rows: 1.5rem;
}

.drop {
    position: absolute;
    z-index: 1;
    inline-size: 100%;
    block-size: 0.5rem;
    margin-block-start: -0.25rem;
    background: #87ceeb;
    border-radius: 1rem;
    opacity: 0;
    pointer-events: none;
}
`;export{n as default};
